---
interface Props {
  image: string;
  brand: string;
  title: string;
  description: string;
}

const { image, brand, title, description } = Astro.props;
---

<div class="bg-default-bg dark:bg-default-fg rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-all duration-300 cursor-pointer hover:scale-105 ease-in-out">
  <div class="aspect-square bg-gray-100 flex items-center justify-center z-10 overflow-hidden">
    <img 
      src={image} 
      alt={title} 
      class="max-h-full object-contain hover:scale-110 transition-all duration-300 overflow-hidden"
      loading="lazy"
    />
  </div>
  
  <div class="p-4 z-20">
    <div class="text-sm text-default-text font-medium mb-1">{brand}</div>
    <h3 class="text-lg font-semibold mb-2">{title}</h3>
    <p class="text-sm text-default-text mb-4">{description}</p>
    
    <button class="bg-success text-white w-full py-2 px-4 rounded-md flex items-center justify-center hover:bg-success-400 transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M18 10c0 3.866-3.582 7-8 7a8.841 8.841 0 01-4.083-.98L2 17l1.338-3.123C2.493 12.767 2 11.434 2 10c0-3.866 3.582-7 8-7s8 3.134 8 7zM7 9H5v2h2V9zm8 0h-2v2h2V9zM9 9h2v2H9V9z" clip-rule="evenodd" />
      </svg>
      Solicitar por WhatsApp
    </button>
  </div>
</div>
